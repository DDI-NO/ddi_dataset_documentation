# Project Structure

This documentation site is built from two sources:

- Hand-edited content under `content/`.
- Generated pages under `generated/` produced by the documentation builder.

The build step assembles a unified `docs/` directory for MkDocs.
