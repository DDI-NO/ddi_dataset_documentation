# Dataset Variables

The flat dataset catalogue merges exportable variables across experiments along with
computed variables defined in `config/computed.yaml`. 

Browse "[Variables](variables.md)" to access the page with the full list of variables. 
